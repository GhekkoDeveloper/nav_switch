import 'package:flutter/material.dart';

class Screen04 extends StatelessWidget {
  const Screen04({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Screen 04', style: TextStyle(fontSize: 24)));
  }
}